version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        build:
          commands:
            - echo "static site, nothing to build"
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths: []
